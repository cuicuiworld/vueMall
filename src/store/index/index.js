import fetch from '@/config/fetch';
import {
    getSessionStorage,
    setSessionStorage,
    getLocalStorage,
    setLocalStorage,
} from '@/config/cache';

export default {
    namespaced: true, // 解决不同模块命名冲突的问题
    state: {

    },
    getters: {

    },
    mutations: {

    },
    actions: {

    }
}